node_modules
dist
out
.DS_Store
.eslintcache
*.log*
.env

# Local Database
*.db
*.sqlite

# Test output
coverage

# AI tools & mémos perso
# -> Suivis sur "origin" (repo perso RootPass) pour les retrouver sur tous mes PC.
# -> Retirés automatiquement du repo "public" (RootPass-App) par release-public.ps1.
# On garde quand même hors de Git les fichiers spécifiques à une machine :
.claude/settings.local.json
.claude/.credentials*